{
  "name": "code-quality-pipeline",
  "version": "1.0.0",
  "description": "Code Quality CI Pipeline with Jenkins",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^8.54.0"
  }
}